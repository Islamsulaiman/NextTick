<h1>Delete Task?</h1>

<p>Are you sure you want to delete this task?</p>

<%= form_for([@project, @task], method: :delete) do |f| %>
  <div class="actions">
    <%= f.submit 'Delete' %>
  </div>
<% end %>

<%= link_to 'Back', project_path(@project) %>